# Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# GCP — server-side (Directions, Geocoding, Places API New)
# Restrict to PinkiPi's public IP in Cloud Console
GOOGLE_MAPS_SERVER_KEY=

# GCP — browser JS Maps key
# Restrict HTTP referrer to: ev.caesartiberius.com/*
GOOGLE_MAPS_JS_KEY=
